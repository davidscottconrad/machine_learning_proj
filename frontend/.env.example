# Optional. Defaults to http://localhost:8000 (matches `uvicorn app:app --port 8000` in
# python/). On Vercel this is injected automatically by the service binding in the root
# vercel.json -- you don't need to set it there.
PREDICTOR_URL=http://localhost:8000
